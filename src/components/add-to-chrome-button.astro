---
import { Icon } from "astro-icon/components";
import { type UtmCampaign, getChromeStoreUrl } from "@/utils/chrome-store-url";

interface Props {
  campaign: UtmCampaign;
}

const { campaign } = Astro.props;
---

<a
  href={getChromeStoreUrl(campaign)}
  target="_blank"
  rel="noopener noreferrer"
  class="group inline-flex items-center gap-3 rounded-xl bg-primary px-8 py-4 font-semibold text-primary-foreground shadow-lg shadow-primary/25 transition-all will-change-transform hover:scale-105 hover:shadow-xl hover:shadow-primary/30"
>
  <Icon name="simple-icons:googlechrome" class="size-6" aria-hidden="true" />
  Add to Chrome — It's Free
  <Icon
    name="lucide:arrow-right"
    class="size-5 transition-transform will-change-transform group-hover:translate-x-1"
    aria-hidden="true"
  />
</a>
